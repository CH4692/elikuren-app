# Archived: Hetzner / Traefik deploy stack

**Status: archived.** Do not use for new deployments.

Target hosting:

- API → Render + Neon (`app-elikuren-api/docs/DEPLOY.md`)
- Web → Vercel (`app-elikuren-web/docs/DEPLOY.md`)

Server Terraform lives in `archive/hetzner-infra/`.
Decommission steps: `archive/README.md`.

Local API Postgres for development now lives in `app-elikuren-api/docker-compose.yml`.
