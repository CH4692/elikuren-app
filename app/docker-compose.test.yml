services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: elikuren
      POSTGRES_PASSWORD: elikuren
      POSTGRES_DB: elikuren
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U elikuren -d elikuren"]
      interval: 5s
      timeout: 5s
      retries: 10
