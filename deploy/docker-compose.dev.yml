# Development – Traefik + Web + API
# Start: docker compose -f docker-compose.dev.yml --env-file .env.dev up -d
# Runtime config + image tags come from .env.dev

services:
  traefik:
    image: traefik:v3.6.8
    container_name: traefik-dev
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik.yml:/etc/traefik/traefik.yml:ro
      - ./letsencrypt:/letsencrypt
      - ./traefik-users:/etc/traefik/traefik-users:ro
    networks:
      - proxy
    labels:
      - traefik.enable=true
      - traefik.docker.network=proxy
      - traefik.http.routers.traefik-dev.rule=Host(`${TRAEFIK_DASHBOARD_HOST}`)
      - traefik.http.routers.traefik-dev.entrypoints=websecure
      - traefik.http.routers.traefik-dev.middlewares=traefik-auth
      - traefik.http.middlewares.traefik-auth.basicauth.usersfile=/etc/traefik/traefik-users
      - traefik.http.routers.traefik-dev.tls=true
      - traefik.http.routers.traefik-dev.tls.certresolver=le
      - traefik.http.routers.traefik-dev.service=api@internal

  web:
    image: ${WEB_IMAGE}
    container_name: elikuren-web-dev
    restart: unless-stopped
    env_file:
      - .env.dev
    healthcheck:
      test:
        ["CMD-SHELL", "curl -fsS http://localhost:3000/api/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 20s
    networks:
      - proxy
    labels:
      - traefik.enable=true
      - traefik.docker.network=proxy
      - traefik.http.routers.web-dev.rule=Host(`${WEB_HOST}`)
      - traefik.http.routers.web-dev.entrypoints=websecure
      - traefik.http.routers.web-dev.tls=true
      - traefik.http.routers.web-dev.tls.certresolver=le
      - traefik.http.services.web-dev.loadbalancer.server.port=3000

  api:
    image: ${API_IMAGE}
    container_name: elikuren-api-dev
    restart: unless-stopped
    env_file:
      - .env.dev
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 20s
    networks:
      - proxy
    labels:
      - traefik.enable=true
      - traefik.docker.network=proxy
      - traefik.http.routers.api-dev.rule=Host(`${API_HOST}`)
      - traefik.http.routers.api-dev.entrypoints=websecure
      - traefik.http.routers.api-dev.tls=true
      - traefik.http.routers.api-dev.tls.certresolver=le
      - traefik.http.services.api-dev.loadbalancer.server.port=8000

networks:
  proxy:
    name: proxy
